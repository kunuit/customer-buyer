const initState = {

};

// import * as actionTypes from '../constants/auth.constants';

export const reducer = (state = initState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};
